[package]
name = "validate-workspace-yaml"
version = "0.1.0"
edition = "2021"
description = "Validates workspace.yaml files in hf-tasks/tasks follow the SWE-bench schema"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"

[dev-dependencies]
tempfile = "3.12"
