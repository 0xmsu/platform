repo: "org/project"
instance_id: "org__project-9999"
base_commit: "999aaa888bbb777"
patch: |
  diff --git a/src/handler.rs b/src/handler.rs
  --- a/src/handler.rs
  +++ b/src/handler.rs
  @@ -5,3 +5,8 @@
  +fn handle_request(req: Request) -> Response {
  +    Response::ok()
  +}
test_patch: |
  diff --git a/tests/test_handler.rs b/tests/test_handler.rs
  --- a/tests/test_handler.rs
  +++ b/tests/test_handler.rs
  @@ -1,3 +1,7 @@
  +#[test]
  +fn test_handle_request() {
  +    let resp = handle_request(Request::new());
  +    assert_eq!(resp.status(), 200);
  +}
problem_statement: "Add request handler function that returns OK response for incoming HTTP requests."
FAIL_TO_PASS:
  - "cargo test test_handle_request"
PASS_TO_PASS:
  - "cargo test test_server_starts"
created_at: "2024-03-10T09:00:00Z"
version: "1.0.0"
difficulty: "hard"
